import { Poppins } from "next/font/google";

export const PoppinsBold = Poppins({
  subsets: ["latin"],
  weight: "700",
});

export const PoppinsSemi = Poppins({
  subsets: ["latin"],
  weight: "600",
});

export const PoppinsRegular = Poppins({
  subsets: ["latin"],
  weight: "400",
});

export const PoppinsLight = Poppins({
  subsets: ["latin"],
  weight: "300",
});
